when_flag_clicked:
    switch_costume(Hammer.png)
    go_to(mouse_x, mouse_y)
    
     forever():
        go_to(mouse_x, mouse_y)
        if(mouse_down):
            switch_costume(HammerHit.png)
        else:
            switch_costume(Hammer.png)