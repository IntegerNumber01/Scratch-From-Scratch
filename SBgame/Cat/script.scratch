

define sure(x):
    change_x(x + 200)
    change_y(y)

when_flag_clicked:
    go_to(0, 0)
    x = 100
    a = x + 10
    if (a < 10):
        go_to(100, 100)
    else:
        if (a == 100):
            set_x(50)
        else:
            go_to(-100,0)